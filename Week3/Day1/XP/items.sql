-- create table items (
-- id serial primary key,
-- desk varchar(100),
-- price INT
-- );

-- insert into items (desk, price) values ('Small Desk', 100);
-- insert into items (desk, price) values ('Large Desk', 300);
-- insert into items (desk, price) values ('Fan', 80);

-- Use SQL to fetch the following data from the database

-- select * from items    --All the items.
-- select * from items where price > 80  --All the items with a price above 80 (80 not included)
-- select * from items where price <= 300 --All the items with a price below 300. (300 included)


